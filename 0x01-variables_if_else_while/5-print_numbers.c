#include <stdio.h>

/**
 * main - display numbers from 0 to 9
 * Return: return 0 and exit the program.
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
